server:
  port: 8082

spring:
  application:
    name: employee-service
  config:
    import: "optional:configserver:http://localhost:8088"
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/davinchicoder

